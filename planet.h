class Planet {
public:
  Planet(int ord, int sz, int min, int atmos) {
    order = ord;
    size = sz;
    minerals = min;
    atmosphere = atmos;
    };

  int order, size, minerals, atmosphere;
  };
